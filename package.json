{
  "name": "billboard-shot",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "sim": "node scripts/sim.mjs",
    "playtest": "node scripts/playtest.mjs",
    "level": "node scripts/level.mjs",
    "art": "node scripts/art.mjs"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "esbuild": "^0.21.5"
  }
}
